apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: notificator
spec:
  replicas: 2
  template:
    metadata:
      labels:
        name: beee-notificator
    spec:
      containers:
      - name: beee-notificator
        image: gcr.io/smartstage-159404/beee-notificator:latest
        env:
          - name: ENV
            value: dev
        ports:
        - containerPort: 8081
          hostPort: 8081
---
apiVersion: v1
kind: Service
metadata:
  name: beee-notificator
  labels:
    name: beee-notificator
spec:
  ports:
  - port: 8081
    targetPort: 8081
  selector:
    name: beee-notificator
